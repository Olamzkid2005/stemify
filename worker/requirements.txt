# Separation engine runtime dependencies (plan Task 9).
# These stay optional until you run real separation: tests and the worker
# control plane run with requirements-dev.txt alone.
#
#   CPU only (default):      pip install -r requirements.txt
#   NVIDIA GPU (optional):   pip install torch==2.5.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu124
#                            pip install -r requirements.txt
#
# torch >= 2.5.1: demucs 4.0.1 checkpoints were serialized with torch <= 2.5
# defaults, and torch 2.6+ resolves an unset weights_only to True, which
# rejects them. The adapter (worker/models/demucs.py) patches torch.load with
# weights_only=False for the duration of each checkpoint load; integrity is
# enforced separately via torch.hub check_hash=True and the profile checksum.
# On Python 3.13 use torch/torchaudio >= 2.6 (2.5.1 has no 3.13 wheels).

demucs==4.0.1
numpy==2.1.3
torch>=2.5.1
torchaudio>=2.5.1
soundfile==0.12.1

# Optional: YouTube input only (plan Task 14). The upload flow never needs
# this; without it, YouTube jobs fail with DOWNLOAD_FAILED (fail safely).
yt-dlp
